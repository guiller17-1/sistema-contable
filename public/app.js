const $ = (s, root=document) => root.querySelector(s);
const $$ = (s, root=document) => [...root.querySelectorAll(s)];
const money = v => `S/ ${Number(v || 0).toLocaleString('es-PE',{minimumFractionDigits:2,maximumFractionDigits:2})}`;
const dateFmt = v => v ? new Date(v+'T00:00:00').toLocaleDateString('es-PE') : '-';
const esc = v => String(v ?? '').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const DB_KEY='uni_contable_demo_v3';
const SESSION_KEY='uni_contable_session_v3';
let cuentas=[];
let currentUser=null;
let selectedAsiento=null;
let navBound=false;

function isoDay(day=1){
  const d=new Date(); d.setDate(Math.min(day,28));
  return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
}
function clone(v){return JSON.parse(JSON.stringify(v));}
function seedDb(){
  const cuentas=[
    {id:1,codigo:'10',nombre:'Caja y Bancos',tipo:'Activo',naturaleza:'DEBE',activa:true},
    {id:2,codigo:'12',nombre:'Cuentas por Cobrar Comerciales',tipo:'Activo',naturaleza:'DEBE',activa:true},
    {id:3,codigo:'20',nombre:'Mercaderías',tipo:'Activo',naturaleza:'DEBE',activa:true},
    {id:4,codigo:'33',nombre:'Propiedad, Planta y Equipo',tipo:'Activo',naturaleza:'DEBE',activa:true},
    {id:5,codigo:'40',nombre:'Tributos / IGV / Retenciones',tipo:'Pasivo',naturaleza:'HABER',activa:true},
    {id:6,codigo:'41',nombre:'Remuneraciones por Pagar',tipo:'Pasivo',naturaleza:'HABER',activa:true},
    {id:7,codigo:'42',nombre:'Cuentas por Pagar Comerciales',tipo:'Pasivo',naturaleza:'HABER',activa:true},
    {id:8,codigo:'50',nombre:'Capital',tipo:'Patrimonio',naturaleza:'HABER',activa:true},
    {id:9,codigo:'60',nombre:'Compras / Gastos',tipo:'Gasto',naturaleza:'DEBE',activa:true},
    {id:10,codigo:'62',nombre:'Gastos de Personal',tipo:'Gasto',naturaleza:'DEBE',activa:true},
    {id:11,codigo:'69',nombre:'Costo de Ventas',tipo:'Gasto',naturaleza:'DEBE',activa:true},
    {id:12,codigo:'70',nombre:'Ventas',tipo:'Ingreso',naturaleza:'HABER',activa:true}
  ];
  const asientos=[
    {id:1,fecha:isoDay(1),numero_asiento:'AS-0001',cuenta:'10 - Caja y Bancos',glosa:'Aporte inicial de capital',tipo_asiento:'Apertura',monto:25000,debe:25000,haber:0,actividad:'Financiamiento',centro_costo:'Administración',documento:'AP-001'},
    {id:2,fecha:isoDay(1),numero_asiento:'AS-0001',cuenta:'50 - Capital',glosa:'Aporte inicial de capital',tipo_asiento:'Apertura',monto:25000,debe:0,haber:25000,actividad:'Financiamiento',centro_costo:'Administración',documento:'AP-001'},
    {id:3,fecha:isoDay(4),numero_asiento:'AS-0002',cuenta:'20 - Mercaderías',glosa:'Compra de mercadería para comercialización',tipo_asiento:'Operación',monto:10000,debe:10000,haber:0,actividad:'Operativa',centro_costo:'Almacén',documento:'F001-1025'},
    {id:4,fecha:isoDay(4),numero_asiento:'AS-0002',cuenta:'40 - Tributos / IGV / Retenciones',glosa:'IGV crédito fiscal de compra',tipo_asiento:'Operación',monto:1800,debe:1800,haber:0,actividad:'Operativa',centro_costo:'Administración',documento:'F001-1025'},
    {id:5,fecha:isoDay(4),numero_asiento:'AS-0002',cuenta:'42 - Cuentas por Pagar Comerciales',glosa:'Obligación con proveedor por compra',tipo_asiento:'Operación',monto:11800,debe:0,haber:11800,actividad:'Operativa',centro_costo:'Administración',documento:'F001-1025'},
    {id:6,fecha:isoDay(8),numero_asiento:'AS-0003',cuenta:'12 - Cuentas por Cobrar Comerciales',glosa:'Venta de 1000 unidades a S/ 10 más IGV',tipo_asiento:'Operación',monto:11800,debe:11800,haber:0,actividad:'Operativa',centro_costo:'Ventas',documento:'F001-0001'},
    {id:7,fecha:isoDay(8),numero_asiento:'AS-0003',cuenta:'70 - Ventas',glosa:'Valor de venta de 1000 unidades',tipo_asiento:'Operación',monto:10000,debe:0,haber:10000,actividad:'Operativa',centro_costo:'Ventas',documento:'F001-0001'},
    {id:8,fecha:isoDay(8),numero_asiento:'AS-0003',cuenta:'40 - Tributos / IGV / Retenciones',glosa:'IGV de la venta',tipo_asiento:'Operación',monto:1800,debe:0,haber:1800,actividad:'Operativa',centro_costo:'Administración',documento:'F001-0001'},
    {id:9,fecha:isoDay(8),numero_asiento:'AS-0004',cuenta:'69 - Costo de Ventas',glosa:'Reconocimiento del costo de la mercadería vendida',tipo_asiento:'Operación',monto:6000,debe:6000,haber:0,actividad:'Operativa',centro_costo:'Ventas',documento:'CV-001'},
    {id:10,fecha:isoDay(8),numero_asiento:'AS-0004',cuenta:'20 - Mercaderías',glosa:'Salida de mercadería por venta',tipo_asiento:'Operación',monto:6000,debe:0,haber:6000,actividad:'Operativa',centro_costo:'Almacén',documento:'CV-001'},
    {id:11,fecha:isoDay(15),numero_asiento:'AS-0005',cuenta:'62 - Gastos de Personal',glosa:'Registro de planilla mensual',tipo_asiento:'Operación',monto:10000,debe:10000,haber:0,actividad:'Operativa',centro_costo:'Administración',documento:'PL-001'},
    {id:12,fecha:isoDay(15),numero_asiento:'AS-0005',cuenta:'41 - Remuneraciones por Pagar',glosa:'Neto de planilla por pagar',tipo_asiento:'Operación',monto:7200,debe:0,haber:7200,actividad:'Operativa',centro_costo:'Administración',documento:'PL-001'},
    {id:13,fecha:isoDay(15),numero_asiento:'AS-0005',cuenta:'40 - Tributos / IGV / Retenciones',glosa:'AFP 13% y retención SUNAT 15%',tipo_asiento:'Operación',monto:2800,debe:0,haber:2800,actividad:'Operativa',centro_costo:'Administración',documento:'PL-001'}
  ];
  const catalogos={
    PRODUCTO_TERMINADO:[{id:1,codigo:'PT-001',nombre:'Producto terminado A',descripcion:'Unidad terminada lista para venta'},{id:2,codigo:'PT-002',nombre:'Producto terminado B',descripcion:'Segunda línea de producto'}],
    MERCADERIA:[{id:1,codigo:'MER-001',nombre:'Mercadería línea A',descripcion:'Stock referencial: 1,500 unidades'},{id:2,codigo:'MER-002',nombre:'Mercadería línea B',descripcion:'Stock referencial: 800 unidades'}],
    ACTIVO_FIJO:[{id:1,codigo:'AF-001',nombre:'Laptop administrativa',descripcion:'Equipo de cómputo'},{id:2,codigo:'AF-002',nombre:'Estantería de almacén',descripcion:'Mobiliario operativo'}],
    CENTRO_COSTO:[{id:1,codigo:'CC-ADM',nombre:'Administración',descripcion:'Gastos administrativos'},{id:2,codigo:'CC-VTA',nombre:'Ventas',descripcion:'Gestión comercial'},{id:3,codigo:'CC-ALM',nombre:'Almacén',descripcion:'Operación y existencias'}],
    INDUCTOR:[{id:1,codigo:'IND-001',nombre:'Horas máquina',descripcion:'Inductor para distribución de CIF'},{id:2,codigo:'IND-002',nombre:'Horas hombre',descripcion:'Inductor de mano de obra'}],
    REPUESTO:[{id:1,codigo:'REP-001',nombre:'Kit de mantenimiento',descripcion:'Repuesto de uso operativo'}]
  };
  return {
    version:'3.0.0-demo', cuentas, asientos, catalogos,
    compras:[{id:1,fecha:isoDay(4),documento:'F001-1025',proveedor:'Proveedor Industrial SAC',ruc:'20123456789',descripcion:'Compra de mercaderías',subtotal:10000,igv:1800,total:11800},{id:2,fecha:isoDay(12),documento:'E001-0088',proveedor:'Servicios Generales Lima SAC',ruc:'20654321987',descripcion:'Servicio de mantenimiento',subtotal:2500,igv:450,total:2950}],
    ventas:[{id:1,fecha:isoDay(8),documento:'F001-0001',cliente:'Juan Pérez',ruc_dni:'45879632',descripcion:'Venta de 1000 unidades a S/ 10',subtotal:10000,igv:1800,total:11800},{id:2,fecha:isoDay(18),documento:'B001-0031',cliente:'María Salazar',ruc_dni:'70445566',descripcion:'Venta minorista de mercadería',subtotal:3200,igv:576,total:3776}],
    planillas:[{id:1,periodo:isoDay(1).slice(0,7),trabajador:'Carlos Mendoza',sueldo_bruto:10000,sistema_pension:'AFP',descuento_pension:1300,retencion_sunat:1500,otros_descuentos:0,neto:7200},{id:2,periodo:isoDay(1).slice(0,7),trabajador:'Ana Torres',sueldo_bruto:4800,sistema_pension:'ONP',descuento_pension:624,retencion_sunat:0,otros_descuentos:100,neto:4076}],
    costos:[{id:1,fecha:isoDay(3),concepto:'Alquiler de planta',tipo:'FIJO',categoria:'CIF',centro_costo:'Producción',monto:2500},{id:2,fecha:isoDay(6),concepto:'Energía de producción',tipo:'VARIABLE',categoria:'CIF',centro_costo:'Producción',monto:1200},{id:3,fecha:isoDay(9),concepto:'Flete de mercadería',tipo:'VARIABLE',categoria:'Distribución',centro_costo:'Ventas',monto:900},{id:4,fecha:isoDay(14),concepto:'Mantenimiento preventivo',tipo:'FIJO',categoria:'CIF',centro_costo:'Producción',monto:650}]
  };
}
function loadDb(){
  const raw=localStorage.getItem(DB_KEY);
  if(!raw){const db=seedDb();localStorage.setItem(DB_KEY,JSON.stringify(db));return db;}
  try{return JSON.parse(raw);}catch(_){const db=seedDb();localStorage.setItem(DB_KEY,JSON.stringify(db));return db;}
}
function saveDb(db){localStorage.setItem(DB_KEY,JSON.stringify(db));}
function nextId(arr){return arr.length?Math.max(...arr.map(x=>Number(x.id)||0))+1:1;}
function parseBody(options){try{return options?.body?JSON.parse(options.body):{};}catch(_){return {};}}
function needSession(){const s=localStorage.getItem(SESSION_KEY);if(!s)throw new Error('NO_SESSION');return JSON.parse(s);}
function sum(arr,field){return arr.reduce((a,x)=>a+Number(x[field]||0),0);}
function accountCode(cuenta){return String(cuenta||'').split(' ')[0];}
function groupBy(arr,keyFn){return arr.reduce((m,x)=>{const k=keyFn(x);(m[k]??=[]).push(x);return m;},{});}
function currentMonth(v){return String(v||'').slice(0,7)===isoDay(1).slice(0,7);}

async function api(url, options={}){
  const method=(options.method||'GET').toUpperCase();
  const body=parseBody(options);
  if(url==='/api/auth/login' && method==='POST'){
    if(body.username==='admin' && body.password==='uni2026'){
      const u={id:1,username:'admin',nombre:'Administrador UNI',rol:'ADMIN'};localStorage.setItem(SESSION_KEY,JSON.stringify(u));return clone(u);
    }
    throw new Error('Credenciales incorrectas');
  }
  if(url==='/api/auth/logout'){localStorage.removeItem(SESSION_KEY);return {ok:true};}
  if(url==='/api/auth/me') return clone(needSession());
  needSession();
  const db=loadDb();

  if(url==='/api/cuentas'){
    if(method==='GET') return clone(db.cuentas.filter(x=>x.activa!==false).sort((a,b)=>a.codigo.localeCompare(b.codigo)));
    if(method==='POST'){
      if(!body.codigo||!body.nombre)throw new Error('Código y nombre son obligatorios');
      if(db.cuentas.some(x=>x.codigo===body.codigo))throw new Error('El código de cuenta ya existe');
      const row={id:nextId(db.cuentas),codigo:body.codigo,nombre:body.nombre,tipo:body.tipo||'Activo',naturaleza:body.naturaleza||'DEBE',activa:true};db.cuentas.push(row);saveDb(db);return clone(row);
    }
  }
  if(url==='/api/asientos' && method==='GET') return clone([...db.asientos].sort((a,b)=>String(b.fecha).localeCompare(String(a.fecha))||b.id-a.id));
  if(url==='/api/asientos' && method==='POST'){
    const row={id:nextId(db.asientos),...body};db.asientos.push(row);saveDb(db);return clone(row);
  }
  let m=url.match(/^\/api\/asientos\/(\d+)$/);
  if(m){const id=Number(m[1]),i=db.asientos.findIndex(x=>Number(x.id)===id);if(i<0)throw new Error('Asiento no encontrado');if(method==='PUT'){db.asientos[i]={...db.asientos[i],...body,id};saveDb(db);return clone(db.asientos[i]);}if(method==='DELETE'){db.asientos.splice(i,1);saveDb(db);return {ok:true};}}

  for(const entity of ['compras','ventas','planillas','costos']){
    if(url===`/api/${entity}` && method==='GET')return clone([...db[entity]].sort((a,b)=>String(b.fecha||b.periodo).localeCompare(String(a.fecha||a.periodo))||b.id-a.id));
    if(url===`/api/${entity}` && method==='POST'){const row={id:nextId(db[entity]),...body};db[entity].push(row);saveDb(db);return clone(row);}
    const rx=new RegExp(`^/api/${entity}/(\\d+)$`);const mm=url.match(rx);if(mm&&method==='DELETE'){const id=Number(mm[1]);db[entity]=db[entity].filter(x=>Number(x.id)!==id);saveDb(db);return {ok:true};}
  }

  m=url.match(/^\/api\/catalogos\/([^/]+)$/);
  if(m){const tipo=decodeURIComponent(m[1]);db.catalogos[tipo]??=[];if(method==='GET')return clone(db.catalogos[tipo]);if(method==='POST'){const row={id:nextId(db.catalogos[tipo]),codigo:body.codigo||'',nombre:body.nombre||'',descripcion:body.descripcion||''};db.catalogos[tipo].push(row);saveDb(db);return clone(row);}}
  m=url.match(/^\/api\/catalogos\/([^/]+)\/(\d+)$/);
  if(m&&method==='DELETE'){const tipo=decodeURIComponent(m[1]),id=Number(m[2]);db.catalogos[tipo]=(db.catalogos[tipo]||[]).filter(x=>Number(x.id)!==id);saveDb(db);return {ok:true};}

  if(url==='/api/dashboard')return {asientos:db.asientos.length,debe:sum(db.asientos,'debe'),haber:sum(db.asientos,'haber'),compras_mes:sum(db.compras.filter(x=>currentMonth(x.fecha)),'total'),ventas_mes:sum(db.ventas.filter(x=>currentMonth(x.fecha)),'total'),costos_mes:sum(db.costos.filter(x=>currentMonth(x.fecha)),'monto')};
  if(url==='/api/reportes/diario')return clone([...db.asientos].sort((a,b)=>String(a.fecha).localeCompare(String(b.fecha))||a.id-b.id));
  if(url==='/api/reportes/mayor'){
    const g=groupBy(db.asientos,x=>x.cuenta);return Object.keys(g).sort().map(cuenta=>({cuenta,movimientos:g[cuenta].length,debe:sum(g[cuenta],'debe'),haber:sum(g[cuenta],'haber'),saldo:sum(g[cuenta],'debe')-sum(g[cuenta],'haber')}));
  }
  if(url==='/api/reportes/balance-comprobacion'){
    const g=groupBy(db.asientos,x=>x.cuenta);return Object.keys(g).sort().map(cuenta=>{const debe=sum(g[cuenta],'debe'),haber=sum(g[cuenta],'haber'),dif=debe-haber;return {cuenta,debe,haber,saldo_deudor:Math.max(dif,0),saldo_acreedor:Math.max(-dif,0)}});
  }
  if(url==='/api/reportes/flujo-efectivo'){
    const g=groupBy(db.asientos,x=>x.actividad||'Sin clasificar');return Object.keys(g).sort().map(actividad=>({actividad,entradas:sum(g[actividad],'debe'),salidas:sum(g[actividad],'haber'),flujo_neto:sum(g[actividad],'debe')-sum(g[actividad],'haber')}));
  }
  if(url==='/api/reportes/resultados'){
    const ingresos=db.asientos.filter(x=>accountCode(x.cuenta).startsWith('7')).reduce((s,x)=>s+Number(x.haber||0)-Number(x.debe||0),0);
    const gastos=db.asientos.filter(x=>accountCode(x.cuenta).startsWith('6')).reduce((s,x)=>s+Number(x.debe||0)-Number(x.haber||0),0);
    return [{concepto:'Ingresos',monto:ingresos},{concepto:'Gastos / Costos',monto:gastos},{concepto:'Resultado del periodo',monto:ingresos-gastos}];
  }
  if(url==='/api/reportes/balance-general'){
    const byType={};for(const c of db.cuentas){const moves=db.asientos.filter(a=>accountCode(a.cuenta)===c.codigo);const saldo=c.naturaleza==='DEBE'?sum(moves,'debe')-sum(moves,'haber'):sum(moves,'haber')-sum(moves,'debe');byType[c.tipo]=(byType[c.tipo]||0)+saldo;}return Object.entries(byType).map(([tipo,saldo])=>({tipo,saldo}));
  }
  if(url==='/api/reportes/costo-ventas'){
    const g=groupBy(db.costos,x=>x.tipo);return Object.keys(g).sort().map(tipo=>({tipo,monto:sum(g[tipo],'monto')}));
  }
  if(url==='/api/cierre/resumen'){const debe=sum(db.asientos,'debe'),haber=sum(db.asientos,'haber');return {debe,haber,diferencia:debe-haber,asientos:db.asientos.length,cuadrado:Math.abs(debe-haber)<0.01};}
  throw new Error(`Ruta no implementada: ${url}`);
}

function showLogin(){currentUser=null;$('#appView').classList.add('hidden');$('#loginView').classList.remove('hidden');$('#username').focus();}
async function enterApp(){
  currentUser=await api('/api/auth/me');
  $('#loginView').classList.add('hidden');$('#appView').classList.remove('hidden');
  $('#userName').textContent=currentUser.nombre||currentUser.username;$('#userRole').textContent=currentUser.rol;
  cuentas=await api('/api/cuentas');if(!navBound){bindNav();navBound=true;}await showDashboard();
}
async function boot(){
  if(!localStorage.getItem(DB_KEY))saveDb(seedDb());
  $('#loginForm').addEventListener('submit',async e=>{e.preventDefault();const box=$('#loginError');box.classList.add('hidden');try{await api('/api/auth/login',{method:'POST',body:JSON.stringify({username:$('#username').value.trim(),password:$('#password').value})});await enterApp();}catch(err){box.textContent=err.message||'No se pudo iniciar sesión';box.classList.remove('hidden');}});
  try{await enterApp();}catch(_){showLogin();}
}
function bindNav(){
  $$('.nav-item[data-page]').forEach(b=>b.addEventListener('click',()=>safeRun(()=>navigatePage(b.dataset.page,b.textContent.trim(),b))));
  $$('.nav-item[data-report]').forEach(b=>b.addEventListener('click',()=>safeRun(()=>showReport(b.dataset.report,b.dataset.title,b))));
  $('#btnLogout').addEventListener('click',async()=>{await api('/api/auth/logout',{method:'POST'});$('#sidebar').classList.remove('open');showLogin();});
  $('#btnMenu').addEventListener('click',()=>$('#sidebar').classList.toggle('open'));
}
async function safeRun(fn){try{await fn();if(innerWidth<760)$('#sidebar').classList.remove('open');}catch(err){console.error(err);setContent(`<div class="card"><h3>No se pudo abrir el módulo</h3><div class="danger-note">${esc(err.message||'Error inesperado')}</div></div>`);}}
function markActive(btn,title){$$('.nav-item').forEach(x=>x.classList.remove('active'));if(btn)btn.classList.add('active');$('#pageTitle').textContent=title;}
function setContent(html){$('#content').innerHTML=html;window.scrollTo({top:0,behavior:'smooth'});}
function toast(msg){const t=document.createElement('div');t.className='toast';t.textContent=msg;document.body.appendChild(t);setTimeout(()=>t.remove(),2200);}

async function navigatePage(page,title,btn){markActive(btn,title);const map={dashboard:showDashboard,asientos:showAsientos,compras:()=>showMovimiento('compras'),ventas:()=>showMovimiento('ventas'),planillas:showPlanillas,costos:showCostos,'hoja-costos':showHojaCostos,inventarios:()=>showCatalogoSingle('MERCADERIA','Inventarios y balances'),tablas:showTablas,ajuste:showAjuste,cierre:showCierre,backup:showBackup,estadisticas:showEstadisticas,manual:showManual};if(map[page])await map[page]();}
async function showDashboard(){
  markActive($('.nav-item[data-page="dashboard"]'),'Panel general');const d=await api('/api/dashboard');const diff=Number(d.debe)-Number(d.haber);
  setContent(`<div class="hero"><div class="dashboard-demo"><span class="data-chip">DATA REFERENCIAL CARGADA</span><span class="muted">Puedes agregar, modificar y borrar registros para probar el sistema.</span></div><h2 style="margin-top:12px">Bienvenido, ${esc(currentUser.nombre||currentUser.username)}</h2><p>Vista general del sistema contable y acceso a los principales módulos.</p></div><div class="kpi-grid">${kpi('Asientos registrados',d.asientos,'Movimientos contables acumulados')}${kpi('Ventas del mes',money(d.ventas_mes),'Registro de ventas')}${kpi('Compras del mes',money(d.compras_mes),'Registro de compras')}${kpi('Diferencia Debe/Haber',money(diff),Math.abs(diff)<.01?'Contabilidad cuadrada':'Revisar descuadre')}</div><div class="card"><div class="card-head"><h3>Módulos del sistema</h3><span class="muted">Todos los módulos del menú ya están habilitados</span></div><div class="module-grid">${moduleCard('Operaciones','Registro y mantenimiento de asientos contables.','asientos')}${moduleCard('Libros contables','Diario, mayor, inventarios, planillas, compras y ventas.','asientos')}${moduleCard('Estados financieros','Balance general, resultados, flujo de efectivo y comprobación.','estadisticas')}${moduleCard('Costos','Costos fijos/variables, libro de fábrica y costo de ventas.','costos')}${moduleCard('Cierre','Ajustes contables y validación del cierre del ejercicio.','cierre')}${moduleCard('Tablas y utilitarios','Maestros, backup, estadísticas y manual de usuario.','tablas')}</div></div>`);
  $$('[data-open]').forEach(b=>b.addEventListener('click',()=>{const target=$(`.nav-item[data-page="${b.dataset.open}"]`);target?.click();}));
}
function kpi(label,value,note){return `<div class="kpi"><div class="kpi-label">${label}</div><div class="kpi-value">${value}</div><div class="kpi-note">${note}</div></div>`;}
function moduleCard(t,p,page){return `<div class="module-card"><h4>${t}</h4><p>${p}</p><button class="btn btn-secondary" data-open="${page}">Abrir módulo</button></div>`;}
function cuentaOptions(selected=''){return cuentas.map(c=>{const v=`${c.codigo} - ${c.nombre}`;return `<option value="${esc(v)}" ${v===selected?'selected':''}>${esc(v)}</option>`}).join('');}

async function showAsientos(){
  const rows=await api('/api/asientos');selectedAsiento=null;
  setContent(`<h2 class="section-title">Registro de Asiento Contable</h2><p class="section-sub">Registra, modifica y elimina movimientos. La información alimenta los libros y reportes.</p><div class="card"><div class="form-grid"><div class="field"><label>Fecha</label><input id="aFecha" type="date" value="${isoDay(new Date().getDate())}"></div><div class="field"><label>N.° asiento</label><input id="aNumero" placeholder="Ej. AS-0006"></div><div class="field span-2"><label>Cuenta contable</label><select id="aCuenta">${cuentaOptions()}</select></div><div class="field span-2"><label>Glosa / descripción</label><input id="aGlosa" placeholder="Descripción del movimiento"></div><div class="field"><label>Tipo de asiento</label><select id="aTipo"><option>Apertura</option><option selected>Operación</option><option>Ajuste</option><option>Reversión</option><option>Cierre</option></select></div><div class="field"><label>Columna destino</label><select id="aDestino"><option>Debe</option><option>Haber</option></select></div><div class="field"><label>Monto</label><input id="aMonto" type="number" min="0" step="0.01" value="0.00"></div><div class="field"><label>Actividad</label><select id="aActividad"><option>Operativa</option><option>Inversión</option><option>Financiamiento</option></select></div><div class="field"><label>Centro de costo</label><input id="aCentro" placeholder="Opcional"></div><div class="field"><label>Documento</label><input id="aDocumento" placeholder="Factura / voucher / otro"></div></div><div class="actions"><button class="btn btn-primary" id="aGuardar">Grabar</button><button class="btn btn-warning" id="aModificar">Modificar</button><button class="btn btn-danger" id="aBorrar">Borrar</button><button class="btn btn-secondary" id="aLimpiar">Limpiar</button></div></div><div class="card"><div class="card-head"><h3>Historial de asientos</h3><span class="muted">Haz clic en una fila para editarla.</span></div>${asientosTable(rows)}</div>`);
  $('#aGuardar').onclick=saveAsiento;$('#aModificar').onclick=updateAsiento;$('#aBorrar').onclick=deleteAsiento;$('#aLimpiar').onclick=clearAsiento;$$('#tablaAsientos tbody tr[data-id]').forEach(tr=>tr.addEventListener('click',()=>loadAsiento(rows.find(x=>String(x.id)===tr.dataset.id),tr)));
}
function asientoPayload(){const m=Number($('#aMonto').value||0),dest=$('#aDestino').value;return {fecha:$('#aFecha').value,numero_asiento:$('#aNumero').value.trim(),cuenta:$('#aCuenta').value,glosa:$('#aGlosa').value.trim(),tipo_asiento:$('#aTipo').value,monto:m,debe:dest==='Debe'?m:0,haber:dest==='Haber'?m:0,actividad:$('#aActividad').value,centro_costo:$('#aCentro').value.trim(),documento:$('#aDocumento').value.trim()};}
async function saveAsiento(){const p=asientoPayload();if(!p.glosa||p.monto<=0)return alert('Ingresa una glosa y un monto mayor a cero.');await api('/api/asientos',{method:'POST',body:JSON.stringify(p)});toast('Asiento registrado');await showAsientos();}
async function updateAsiento(){if(!selectedAsiento)return alert('Selecciona un asiento.');await api(`/api/asientos/${selectedAsiento.id}`,{method:'PUT',body:JSON.stringify(asientoPayload())});toast('Asiento actualizado');await showAsientos();}
async function deleteAsiento(){if(!selectedAsiento)return alert('Selecciona un asiento.');if(!confirm(`¿Eliminar el asiento #${selectedAsiento.id}?`))return;await api(`/api/asientos/${selectedAsiento.id}`,{method:'DELETE'});toast('Asiento eliminado');await showAsientos();}
function clearAsiento(){selectedAsiento=null;$('#aGlosa').value='';$('#aMonto').value='0.00';$('#aNumero').value='';$('#aCentro').value='';$('#aDocumento').value='';$$('#tablaAsientos tr').forEach(x=>x.classList.remove('row-selected'));}
function loadAsiento(a,tr){if(!a)return;selectedAsiento=a;$('#aFecha').value=(a.fecha||'').slice(0,10);$('#aNumero').value=a.numero_asiento||'';$('#aCuenta').value=a.cuenta;$('#aGlosa').value=a.glosa;$('#aTipo').value=a.tipo_asiento;$('#aDestino').value=Number(a.debe)>0?'Debe':'Haber';$('#aMonto').value=a.monto;$('#aActividad').value=a.actividad||'Operativa';$('#aCentro').value=a.centro_costo||'';$('#aDocumento').value=a.documento||'';$$('#tablaAsientos tr').forEach(x=>x.classList.remove('row-selected'));tr.classList.add('row-selected');}
function asientosTable(rows){return `<div class="table-wrap"><table id="tablaAsientos"><thead><tr><th>ID</th><th>Fecha</th><th>N.°</th><th>Cuenta</th><th>Glosa</th><th>Tipo</th><th>Debe</th><th>Haber</th><th>Actividad</th></tr></thead><tbody>${rows.map(a=>`<tr data-id="${a.id}"><td>${a.id}</td><td>${dateFmt((a.fecha||'').slice(0,10))}</td><td>${esc(a.numero_asiento||'-')}</td><td><b>${esc(a.cuenta)}</b></td><td>${esc(a.glosa)}</td><td>${esc(a.tipo_asiento)}</td><td class="money">${money(a.debe)}</td><td class="money">${money(a.haber)}</td><td>${esc(a.actividad||'-')}</td></tr>`).join('')||`<tr><td colspan="9" class="empty">No hay asientos registrados.</td></tr>`}</tbody></table></div>`;}

async function showMovimiento(tipo){
  const isCompra=tipo==='compras',rows=await api(`/api/${tipo}`),title=isCompra?'Registro de Compras':'Registro de Ventas';
  setContent(`<h2 class="section-title">${title}</h2><p class="section-sub">Registro auxiliar para control y análisis.</p><div class="card"><div class="form-grid"><div class="field"><label>Fecha</label><input id="mFecha" type="date" value="${isoDay(new Date().getDate())}"></div><div class="field"><label>Documento</label><input id="mDoc" placeholder="F001-0001"></div><div class="field"><label>${isCompra?'Proveedor':'Cliente'}</label><input id="mTercero"></div><div class="field"><label>${isCompra?'RUC':'RUC / DNI'}</label><input id="mRuc"></div><div class="field span-2"><label>Descripción</label><input id="mDesc"></div><div class="field"><label>Subtotal</label><input id="mSub" type="number" step="0.01" value="0"></div><div class="field"><label>IGV</label><input id="mIgv" type="number" step="0.01" value="0"></div><div class="field"><label>Total</label><input id="mTotal" type="number" step="0.01" value="0"></div></div><div class="actions"><button class="btn btn-primary" id="mSave">Guardar</button></div></div><div class="card">${simpleTable(rows,[['fecha','Fecha'],['documento','Documento'],[isCompra?'proveedor':'cliente',isCompra?'Proveedor':'Cliente'],[isCompra?'ruc':'ruc_dni',isCompra?'RUC':'RUC/DNI'],['descripcion','Descripción'],['subtotal','Subtotal','money'],['igv','IGV','money'],['total','Total','money']],tipo)}</div>`);
  $('#mSub').addEventListener('input',calcTotal);$('#mIgv').addEventListener('input',calcTotal);$('#mSave').onclick=async()=>{const p={fecha:$('#mFecha').value,documento:$('#mDoc').value.trim(),[isCompra?'proveedor':'cliente']:$('#mTercero').value.trim(),[isCompra?'ruc':'ruc_dni']:$('#mRuc').value.trim(),descripcion:$('#mDesc').value.trim(),subtotal:Number($('#mSub').value||0),igv:Number($('#mIgv').value||0),total:Number($('#mTotal').value||0)};if(!p[isCompra?'proveedor':'cliente'])return alert('Completa el tercero.');await api(`/api/${tipo}`,{method:'POST',body:JSON.stringify(p)});toast('Registro guardado');await showMovimiento(tipo)};bindDeletes(tipo,()=>showMovimiento(tipo));
}
function calcTotal(){$('#mTotal').value=(Number($('#mSub').value||0)+Number($('#mIgv').value||0)).toFixed(2);}

async function showPlanillas(){const rows=await api('/api/planillas');setContent(`<h2 class="section-title">Planillas</h2><p class="section-sub">Incluye sueldo bruto y deducciones configurables de AFP/ONP, SUNAT y otros conceptos.</p><div class="card"><div class="form-grid"><div class="field"><label>Periodo</label><input id="pPeriodo" type="month" value="${isoDay(1).slice(0,7)}"></div><div class="field span-2"><label>Trabajador</label><input id="pTrab"></div><div class="field"><label>Sueldo bruto</label><input id="pSueldo" type="number" step=".01" value="0"></div><div class="field"><label>Sistema</label><select id="pSis"><option>AFP</option><option>ONP</option></select></div><div class="field"><label>AFP / ONP</label><input id="pPen" type="number" step=".01" value="0"></div><div class="field"><label>Retención SUNAT</label><input id="pSunat" type="number" step=".01" value="0"></div><div class="field"><label>Otros descuentos</label><input id="pOtros" type="number" step=".01" value="0"></div><div class="field"><label>Neto</label><input id="pNeto" type="number" step=".01" value="0" readonly></div></div><div class="actions"><button class="btn btn-primary" id="pSave">Guardar planilla</button></div></div><div class="card">${simpleTable(rows,[['periodo','Periodo'],['trabajador','Trabajador'],['sueldo_bruto','Sueldo bruto','money'],['sistema_pension','Sistema'],['descuento_pension','AFP/ONP','money'],['retencion_sunat','SUNAT','money'],['otros_descuentos','Otros','money'],['neto','Neto','money']],'planillas')}</div>`);const calc=()=>$('#pNeto').value=Math.max(0,Number($('#pSueldo').value||0)-Number($('#pPen').value||0)-Number($('#pSunat').value||0)-Number($('#pOtros').value||0)).toFixed(2);['#pSueldo','#pPen','#pSunat','#pOtros'].forEach(x=>$(x).oninput=calc);$('#pSave').onclick=async()=>{const p={periodo:$('#pPeriodo').value,trabajador:$('#pTrab').value.trim(),sueldo_bruto:Number($('#pSueldo').value||0),sistema_pension:$('#pSis').value,descuento_pension:Number($('#pPen').value||0),retencion_sunat:Number($('#pSunat').value||0),otros_descuentos:Number($('#pOtros').value||0),neto:Number($('#pNeto').value||0)};if(!p.trabajador)return alert('Ingresa el trabajador.');await api('/api/planillas',{method:'POST',body:JSON.stringify(p)});toast('Planilla guardada');await showPlanillas()};bindDeletes('planillas',showPlanillas);}
async function showCostos(){const rows=await api('/api/costos');setContent(`<h2 class="section-title">Costos</h2><p class="section-sub">Libro mayor de fábrica y clasificación de costos fijos y variables.</p><div class="card"><div class="form-grid"><div class="field"><label>Fecha</label><input id="cFecha" type="date" value="${isoDay(new Date().getDate())}"></div><div class="field span-2"><label>Concepto</label><input id="cConcepto"></div><div class="field"><label>Tipo</label><select id="cTipo"><option value="FIJO">Fijo</option><option value="VARIABLE">Variable</option></select></div><div class="field"><label>Categoría</label><input id="cCat" placeholder="Materia prima, MOD, CIF..."></div><div class="field"><label>Centro de costo</label><input id="cCentro"></div><div class="field"><label>Monto</label><input id="cMonto" type="number" step=".01" value="0"></div></div><div class="actions"><button class="btn btn-primary" id="cSave">Registrar costo</button></div></div><div class="card">${simpleTable(rows,[['fecha','Fecha'],['concepto','Concepto'],['tipo','Tipo'],['categoria','Categoría'],['centro_costo','Centro de costo'],['monto','Monto','money']],'costos')}</div>`);$('#cSave').onclick=async()=>{const p={fecha:$('#cFecha').value,concepto:$('#cConcepto').value.trim(),tipo:$('#cTipo').value,categoria:$('#cCat').value.trim(),centro_costo:$('#cCentro').value.trim(),monto:Number($('#cMonto').value||0)};if(!p.concepto)return alert('Ingresa el concepto.');await api('/api/costos',{method:'POST',body:JSON.stringify(p)});toast('Costo registrado');await showCostos()};bindDeletes('costos',showCostos);}
async function showHojaCostos(){const rows=await api('/api/costos');const total=rows.reduce((s,x)=>s+Number(x.monto||0),0),fijo=rows.filter(x=>x.tipo==='FIJO').reduce((s,x)=>s+Number(x.monto||0),0),variable=total-fijo;setContent(`<h2 class="section-title">Hoja de costos</h2><p class="section-sub">Resumen de costos registrados para fines académicos y de análisis.</p><div class="kpi-grid">${kpi('Costo total',money(total),'Acumulado')}${kpi('Costos fijos',money(fijo),'Clasificación FIJO')}${kpi('Costos variables',money(variable),'Clasificación VARIABLE')}${kpi('Registros',rows.length,'Movimientos de costos')}</div><div class="card">${simpleTable(rows,[['fecha','Fecha'],['concepto','Concepto'],['tipo','Tipo'],['categoria','Categoría'],['centro_costo','Centro de costo'],['monto','Monto','money']])}</div>`);}

async function showReport(type,title,btn){markActive(btn,title);const rows=await api(`/api/reportes/${type}`);setContent(`<h2 class="section-title">${esc(title)}</h2><p class="section-sub">Reporte generado con la información registrada en el sistema.</p><div class="card"><div class="card-head"><h3>${esc(title)}</h3><div class="report-tools"><button class="btn btn-secondary" onclick="window.print()">Imprimir</button></div></div><div class="report-box">${reportTable(type,rows)}</div></div>`);}
function reportTable(type,rows){const schemas={diario:[['fecha','Fecha'],['id','ID'],['numero_asiento','N.°'],['cuenta','Cuenta'],['glosa','Glosa'],['documento','Documento'],['debe','Debe','money'],['haber','Haber','money']],mayor:[['cuenta','Cuenta'],['movimientos','Movimientos'],['debe','Debe','money'],['haber','Haber','money'],['saldo','Saldo','money']],'balance-comprobacion':[['cuenta','Cuenta'],['debe','Debe','money'],['haber','Haber','money'],['saldo_deudor','Saldo deudor','money'],['saldo_acreedor','Saldo acreedor','money']],'flujo-efectivo':[['actividad','Actividad'],['entradas','Entradas','money'],['salidas','Salidas','money'],['flujo_neto','Flujo neto','money']],resultados:[['concepto','Concepto'],['monto','Monto','money']],'balance-general':[['tipo','Clasificación'],['saldo','Saldo','money']],'costo-ventas':[['tipo','Tipo'],['monto','Monto','money']]};return simpleTable(rows,schemas[type]||Object.keys(rows[0]||{}).map(k=>[k,k]));}
async function showCatalogoSingle(tipo,title){const rows=await api(`/api/catalogos/${tipo}`);setContent(`<h2 class="section-title">${title}</h2><p class="section-sub">Catálogo para el control de existencias y saldos auxiliares.</p><div class="card"><div class="form-grid"><div class="field"><label>Código</label><input id="gCodigo"></div><div class="field span-2"><label>Nombre</label><input id="gNombre"></div><div class="field"><label>Descripción</label><input id="gDesc"></div></div><div class="actions"><button class="btn btn-primary" id="gSave">Agregar</button></div></div><div class="card">${simpleTable(rows,[['codigo','Código'],['nombre','Nombre'],['descripcion','Descripción']],`catalogos/${tipo}`)}</div>`);$('#gSave').onclick=async()=>{if(!$('#gNombre').value.trim())return alert('Ingresa el nombre.');await api(`/api/catalogos/${tipo}`,{method:'POST',body:JSON.stringify({codigo:$('#gCodigo').value.trim(),nombre:$('#gNombre').value.trim(),descripcion:$('#gDesc').value.trim()})});toast('Registro agregado');await showCatalogoSingle(tipo,title)};bindDeletes(`catalogos/${tipo}`,()=>showCatalogoSingle(tipo,title));}
const catalogTypes=[['CUENTA','Cuentas contables'],['PRODUCTO_TERMINADO','Productos terminados'],['MERCADERIA','Mercaderías'],['ACTIVO_FIJO','Activo fijo'],['CENTRO_COSTO','Centros de costo'],['INDUCTOR','Inductores'],['REPUESTO','Repuestos']];
async function showTablas(){setContent(`<h2 class="section-title">Tablas maestras</h2><p class="section-sub">Mantén los catálogos principales del sistema.</p><div class="catalog-grid"><div class="card"><div class="catalog-menu">${catalogTypes.map((x,i)=>`<button class="${i===0?'active':''}" data-cat="${x[0]}">${x[1]}</button>`).join('')}</div></div><div id="catalogBody"></div></div>`);$$('.catalog-menu button').forEach(b=>b.onclick=()=>loadCatalogPanel(b.dataset.cat,b));await loadCatalogPanel('CUENTA',$('.catalog-menu button'));}
async function loadCatalogPanel(tipo,btn){$$('.catalog-menu button').forEach(x=>x.classList.remove('active'));btn?.classList.add('active');const title=(catalogTypes.find(x=>x[0]===tipo)||[])[1]||tipo;if(tipo==='CUENTA'){const rows=await api('/api/cuentas');$('#catalogBody').innerHTML=`<div class="card"><h3>${title}</h3><div class="form-grid"><div class="field"><label>Código</label><input id="tcCod"></div><div class="field span-2"><label>Nombre</label><input id="tcNom"></div><div class="field"><label>Tipo</label><select id="tcTipo"><option>Activo</option><option>Pasivo</option><option>Patrimonio</option><option>Ingreso</option><option>Gasto</option></select></div><div class="field"><label>Naturaleza</label><select id="tcNat"><option value="DEBE">Debe</option><option value="HABER">Haber</option></select></div></div><div class="actions"><button class="btn btn-primary" id="tcSave">Agregar cuenta</button></div>${simpleTable(rows,[['codigo','Código'],['nombre','Nombre'],['tipo','Tipo'],['naturaleza','Naturaleza']])}</div>`;$('#tcSave').onclick=async()=>{await api('/api/cuentas',{method:'POST',body:JSON.stringify({codigo:$('#tcCod').value.trim(),nombre:$('#tcNom').value.trim(),tipo:$('#tcTipo').value,naturaleza:$('#tcNat').value})});cuentas=await api('/api/cuentas');toast('Cuenta agregada');await loadCatalogPanel('CUENTA',btn)};return;}const rows=await api(`/api/catalogos/${tipo}`);$('#catalogBody').innerHTML=`<div class="card"><h3>${title}</h3><div class="form-grid"><div class="field"><label>Código</label><input id="tcCod"></div><div class="field span-2"><label>Nombre</label><input id="tcNom"></div><div class="field"><label>Descripción</label><input id="tcDesc"></div></div><div class="actions"><button class="btn btn-primary" id="tcSave">Agregar registro</button></div>${simpleTable(rows,[['codigo','Código'],['nombre','Nombre'],['descripcion','Descripción']],`catalogos/${tipo}`)}</div>`;$('#tcSave').onclick=async()=>{await api(`/api/catalogos/${tipo}`,{method:'POST',body:JSON.stringify({codigo:$('#tcCod').value.trim(),nombre:$('#tcNom').value.trim(),descripcion:$('#tcDesc').value.trim()})});toast('Registro agregado');await loadCatalogPanel(tipo,btn)};bindDeletes(`catalogos/${tipo}`,()=>loadCatalogPanel(tipo,btn));}
async function showAjuste(){setContent(`<h2 class="section-title">Ajuste contable</h2><p class="section-sub">Registra un asiento de tipo “Ajuste” antes del cierre.</p><div class="card"><div class="note">Los ajustes se guardan dentro del libro de asientos y quedan identificados con tipo de asiento “Ajuste”.</div><div class="actions"><button class="btn btn-primary" id="goAjuste">Crear ajuste</button></div></div>`);$('#goAjuste').onclick=async()=>{await showAsientos();$('#aTipo').value='Ajuste';$('#pageTitle').textContent='Ajuste contable'};}
async function showCierre(){const d=await api('/api/cierre/resumen');setContent(`<h2 class="section-title">Cierre del ejercicio</h2><p class="section-sub">Validación previa al cierre contable.</p><div class="card"><div class="big-status">${d.cuadrado?'CUADRADO':'REVISAR'}</div><div class="status-line"><span>Total Debe</span><b>${money(d.debe)}</b></div><div class="status-line"><span>Total Haber</span><b>${money(d.haber)}</b></div><div class="status-line"><span>Diferencia</span><b>${money(d.diferencia)}</b></div><div class="status-line"><span>Asientos</span><b>${d.asientos}</b></div><div class="note" style="margin-top:15px">Esta pantalla valida el equilibrio Debe/Haber. El cierre definitivo no elimina ni modifica información automáticamente.</div></div>`);}
function downloadBackup(){const db=loadDb();const payload={generado_en:new Date().toISOString(),tipo:'demo-local',...db};const blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`backup-contable-${new Date().toISOString().slice(0,10)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500);}
async function showBackup(){setContent(`<h2 class="section-title">Backup</h2><p class="section-sub">Descarga una copia JSON de los datos de esta versión demostrativa.</p><div class="card"><h3>Copia de seguridad local</h3><p class="muted">Incluye asientos, cuentas, catálogos, compras, ventas, planillas y costos.</p><div class="actions"><button class="btn btn-primary" id="downloadBackup">Descargar backup</button><button class="btn btn-danger" id="resetDemo">Restaurar data referencial</button></div><div class="notice" style="margin-top:14px">En esta versión Cloudflare de demostración los datos se guardan en el navegador. Más adelante se puede conectar Cloudflare D1 o PostgreSQL para que la información sea compartida entre equipos.</div></div>`);$('#downloadBackup').onclick=downloadBackup;$('#resetDemo').onclick=async()=>{if(!confirm('¿Restaurar la data referencial? Los cambios locales de prueba se reemplazarán.'))return;saveDb(seedDb());cuentas=await api('/api/cuentas');toast('Data referencial restaurada');await showBackup();};}
async function showEstadisticas(){const d=await api('/api/dashboard'),mayor=await api('/api/reportes/mayor');setContent(`<h2 class="section-title">Estadísticas</h2><p class="section-sub">Indicadores generales de la operación contable.</p><div class="kpi-grid">${kpi('Asientos',d.asientos,'Total acumulado')}${kpi('Debe',money(d.debe),'Acumulado')}${kpi('Haber',money(d.haber),'Acumulado')}${kpi('Cuentas con movimiento',mayor.length,'Libro mayor')}</div><div class="card"><h3>Movimiento por cuenta</h3>${simpleTable(mayor,[['cuenta','Cuenta'],['movimientos','Movimientos'],['debe','Debe','money'],['haber','Haber','money'],['saldo','Saldo','money']])}</div>`);}
function showManual(){setContent(`<h2 class="section-title">Manual de usuario</h2><p class="section-sub">Guía rápida del sistema.</p><div class="card"><h3>Flujo recomendado</h3><ol style="line-height:1.8;color:#4b5563"><li>Configura las tablas maestras: cuentas, productos, mercaderías, activos, centros de costo, inductores y repuestos.</li><li>Registra las operaciones mediante asientos contables y, cuando corresponda, usa los auxiliares de compras, ventas, planillas y costos.</li><li>Consulta Libro Diario, Libro Mayor y Balance de Comprobación para validar movimientos.</li><li>Revisa los estados financieros y el flujo de efectivo.</li><li>Antes de cerrar, registra ajustes y verifica que Debe y Haber estén cuadrados.</li><li>Descarga un backup periódico desde Utilitarios.</li></ol><div class="notice">Esta V3 está preparada para desplegarse como sitio estático en Cloudflare y guardar datos de prueba en el navegador. Para producción, la siguiente etapa es una base de datos en la nube y autenticación de servidor.</div></div>`);}
function simpleTable(rows,cols,deleteEndpoint=''){const th=cols.map(c=>`<th>${c[1]}</th>`).join('')+(deleteEndpoint?'<th></th>':'');const body=rows.map(r=>`<tr>${cols.map(c=>{let v=r[c[0]];if(c[2]==='money')v=money(v);else if(c[0]==='fecha')v=dateFmt(String(v||'').slice(0,10));else v=esc(v??'-');return `<td class="${c[2]==='money'?'money':''}">${v}</td>`}).join('')}${deleteEndpoint?`<td><button class="btn btn-danger btn-del" data-id="${r.id}" data-endpoint="${deleteEndpoint}">Eliminar</button></td>`:''}</tr>`).join('')||`<tr><td colspan="${cols.length+(deleteEndpoint?1:0)}" class="empty">Sin registros.</td></tr>`;return `<div class="table-wrap"><table><thead><tr>${th}</tr></thead><tbody>${body}</tbody></table></div>`;}
function bindDeletes(endpoint,cb){$$('.btn-del').forEach(b=>b.onclick=async()=>{if(!confirm('¿Eliminar este registro?'))return;await api(`/api/${endpoint}/${b.dataset.id}`,{method:'DELETE'});toast('Registro eliminado');await cb();});}

boot();
